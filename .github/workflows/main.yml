name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Node dependencies
        run: npm ci

      - name: Run Node tests
        shell: bash
        run: |
          set -o pipefail
          mkdir -p .ci-evidence
          npm run test:ci 2>&1 | tee .ci-evidence/node-tests.txt

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache-dependency-path: apps/evt-api-go/go.sum

      - name: Run Go tests
        shell: bash
        working-directory: apps/evt-api-go
        run: |
          set -o pipefail
          go test ./... 2>&1 | tee ../../.ci-evidence/go-tests.txt

      - name: Verify API Docker build identity
        shell: bash
        run: |
          set -euo pipefail

          docker build \
            --target build \
            --build-arg BUILD_SHA="${GITHUB_SHA}" \
            -t cvera-api:ci-builder \
            apps/evt-api-go

          container_id="$(docker create cvera-api:ci-builder)"
          trap 'docker rm -f "${container_id}" >/dev/null 2>&1 || true' EXIT

          docker cp \
            "${container_id}:/out/evt-api-go" \
            /tmp/evt-api-go-ci

          if ! strings /tmp/evt-api-go-ci | grep -Fq "${GITHUB_SHA}"; then
            echo "ERROR: Docker-built API binary does not contain expected Git SHA"
            exit 1
          fi

          echo "Verified Docker-built API revision identity: ${GITHUB_SHA}"

      - name: Create CI evidence manifest
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        shell: bash
        run: |
          CI_TIMESTAMP="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          CI_EVIDENCE_DATE="${CI_TIMESTAMP%%T*}"

          export CI_TIMESTAMP
          export CI_EVIDENCE_DATE

          echo "CI_TIMESTAMP=$CI_TIMESTAMP" >> "$GITHUB_ENV"
          echo "CI_EVIDENCE_DATE=$CI_EVIDENCE_DATE" >> "$GITHUB_ENV"

          node - <<'NODE'
          const fs = require("fs");

          const manifest = {
            schema_version: 1,
            repository: process.env.GITHUB_REPOSITORY,
            git_sha: process.env.GITHUB_SHA,
            git_ref: process.env.GITHUB_REF,
            workflow: process.env.GITHUB_WORKFLOW,
            workflow_run_id: process.env.GITHUB_RUN_ID,
            workflow_run_attempt: process.env.GITHUB_RUN_ATTEMPT,
            tested_at: process.env.CI_TIMESTAMP,
            node: {
              command: "npm run test:ci",
              result: "passed"
            },
            go: {
              version_contract: "1.25",
              command: "go test ./...",
              result: "passed"
            },
            overall_result: "passed"
          };

          fs.writeFileSync(
            ".ci-evidence/manifest.json",
            JSON.stringify(manifest, null, 2) + "\n"
          );
          NODE

      - name: Archive CI evidence to DigitalOcean Spaces
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        env:
          DO_SPACES_KEY: ${{ secrets.DO_SPACES_KEY }}
          DO_SPACES_SECRET: ${{ secrets.DO_SPACES_SECRET }}
          DO_SPACE: ${{ vars.DO_SPACE }}
          DO_REGION: ${{ vars.DO_REGION }}
          DO_ENDPOINT: ${{ vars.DO_ENDPOINT }}
          CI_EVIDENCE_DATE: ${{ env.CI_EVIDENCE_DATE }}
        run: node scripts/archive-ci-evidence.mjs
